{
    "manage_group": {
        "description": "管理群",
        "type": "string",
        "hint": "bot收到的投稿将在管理群里审批，不填则将审批信息私发给bot管理员"
    },
    "pillowmd_style_dir": {
        "description": "Pillowmd样式目录路径",
        "type": "string",
        "hint": "例如 C:\\styles\\my-style 或 /data/styles/default, 留空则使用默认样式。pillowmd用于渲染说说内容",
        "default": ""
    },
    "comment_prompt": {
        "description": "评论说说的llm提示词",
        "type": "text",
        "hint": "建议自行根据Bot人格进行改写，从而达到更好的效果",
        "default": "请根据帖子内容生成一条精辟简短的评论, 评论要抓住主题"
    },
    "diary_prompt": {
        "description": "写说说时用的llm提示词",
        "type": "text",
        "hint": "建议自行根据Bot人格进行改写，从而达到更好的效果",
        "default": "## 找出一个你感兴趣的主题来写一个段落。要求：- 首句抛出一个备受争议的问题。- 必须围绕抛出的问题进行展开，无关内容不要硬塞。- 剖析问题后不做总结，而是留下个更深层次的叩问。- 充满思辨，保证逻辑性。- 每个单句都换行，方便阅读"
    },
    "diary_max_msg": {
        "description": "用于写说说的聊天记录的最大条数",
        "type": "int",
        "hint": "过长会爆上下文，一般几百条足矣",
        "default": 500
    },
    "publish_cron": {
        "description": "自动发说说时间间隔",
        "type": "string",
        "hint": "格式为0 0 0 0 0，表示分、时、日、月、周，*表示任意，留空表示不启用此定时任务。示例：“30 23 * * *” 表示每天23点半点执行一次",
        "default": "30 23 * * *"
    },
    "comment_cron": {
        "description": "自动评论时间间隔",
        "type": "string",
        "hint": "格式同上。留空表示不启用此定时任务。 示例：“0 8 * * 1” 表示每周一早上8点执行一次",
        "default": "0 8 * * 1"
    },
    "recent_page": {
        "description": "获取好友最近说说时查询的页数",
        "type": "int",
        "hint": "暂时是无效配置！！！每页正常情况下是5条说说，此项配置用于定时自动点赞、评论，注意：评论过的说说会自动跳过",
        "default": 1
    },
    "campus_wall_switch": {
        "description": "校园墙开关",
        "type": "bool",
        "hint": "是否开启校园墙功能，功能包括：投稿、查看稿件、通过投稿、拒绝投稿",
        "default": true
    }
}